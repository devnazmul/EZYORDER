export const logApiResponse = (key: string, response: any) => {
  console.log("-------------------------------------");
  console.log(key.toUpperCase(), response);
  console.log("-------------------------------------");
};
